name: Issue Flow
on: issues
jobs:
    show-issue:
        runs-on: ubuntu-latest
        steps:
            - name: Show Details
              run: echo "${{ toJson(github) }}"
            - name: Issue Details
              run: echo "${{ toJson(github.event) }}"